apiVersion: 1

providers:
  - name: AutoVerdict
    orgId: 1
    folder: AutoVerdict
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
